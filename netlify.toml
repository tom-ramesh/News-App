[build]
    command = "npm run build"
    publish = "dist"
    branches = { only = ["main"]}
    functions = "functions/"

[dev]
    command = "npm run start"
    port = 4000

[dev.environment]
    JSON_SERVER_PORT = "4001"

[[redirects]]
    from = "/api/*"
    to = "/.netlify/functions/:splat"
    status = 200